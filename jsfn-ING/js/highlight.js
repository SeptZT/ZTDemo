function highLight(obj, keywords, cssClass){
  if (!obj || keyword.length == 0){
    return false;
  }

  if (!cssClass){
    var cssClass = "highlight";
  }

  var pattern = new RegExp(keywords, 'gi');
}
